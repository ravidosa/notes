# Convexity
convex: segment between vertices contained in shape
intersection of convex is convex
	convex hull: intersection of all convex sets containing
	if inside, nonnegative linear combination
		caratheodory's theorem: in convex hull in $d$ dimensions, at most $d+1$ points in combo
	radon's lemma: break $d + 2$ points into disjoint convex hulls
	helly: if intersection of every $d + 1$ convex sets in $d$ dimensions is nonempty, then all intersect