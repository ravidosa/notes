# Instruction Set Architecture
instruction set: commands understood by architecture
operations and operands
	word: 32 bit group
		alignment restriction (data in memory must be aligned to word boundaries)
signed vs unsigned numbers
	sign in MSB
	sign and magnitude vs two's complement
keep it simple!
instruction fields
	opcode
	registers (source, target, destination)
	shift
	function code