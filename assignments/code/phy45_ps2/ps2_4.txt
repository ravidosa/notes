The code above approximates integral of f(x) from x=a to x=b with user-inputted integer N segments.
x = a + j * dx increments the segment being computed.
integral = integral + f ∗ dx adds the computed segment to the total.